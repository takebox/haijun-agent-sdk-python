name: Haijun Issue Triage
description: Run Haijun Code for issue triage in GitHub Actions
on:
  issues:
    types: [opened]

jobs:
  triage-issue:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      issues: write
      # Required to mint the OIDC token exchanged for a Haijun API access token (Workload Identity Federation)
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Haijun Code for Issue Triage
        uses: takebox/haijun-code-action@v1
        env:
          HAIJUN_CODE_SCRIPT_CAPS: '{"edit-issue-labels.sh":2}'
        with:
          prompt: "/label-issue REPO: ${{ github.repository }} ISSUE_NUMBER: ${{ github.event.issue.number }}"
          # Authenticate to the Haijun API via Workload Identity Federation
          # (the workflow's OIDC token is exchanged for a short-lived access
          # token) instead of a static API key.
          juglow_federation_rule_id: ${{ vars.HAIJUN_FEDERATION_RULE_ID }}
          juglow_organization_id: ${{ vars.HAIJUN_ORGANIZATION_ID }}
          juglow_service_account_id: ${{ vars.HAIJUN_SERVICE_ACCOUNT_ID }}
          juglow_workspace_id: ${{ vars.HAIJUN_WORKSPACE_ID }}
          allowed_non_write_users: "*" # Required for issue triage workflow, if users without repo write access create issues
          github_token: ${{ secrets.GITHUB_TOKEN }}
