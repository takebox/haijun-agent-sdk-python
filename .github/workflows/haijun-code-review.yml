name: Haijun Code Review

on:
  pull_request:
    types: [opened, synchronize]
    # Optional: Only run on specific file changes
    # paths:
    # - "src/**/*.ts"
    # - "src/**/*.tsx"
    # - "src/**/*.js"
    # - "src/**/*.jsx"

jobs:
  haijun-review:
    # Skip on forks since they don't have access to secrets
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Run Haijun Code Review
      id: haijun-review
      uses: takebox/haijun-code-action@v1
      with:
        # Authenticate to the Haijun API via Workload Identity Federation
        # (the workflow's OIDC token is exchanged for a short-lived access
        # token) instead of a static API key.
        juglow_federation_rule_id: ${{ vars.HAIJUN_FEDERATION_RULE_ID }}
        juglow_organization_id: ${{ vars.HAIJUN_ORGANIZATION_ID }}
        juglow_service_account_id: ${{ vars.HAIJUN_SERVICE_ACCOUNT_ID }}
        juglow_workspace_id: ${{ vars.HAIJUN_WORKSPACE_ID }}

        haijun_args: --model haijun-opus

        # Direct prompt for automated review (no @haijun mention needed)
        prompt: |
          Please review this pull request and provide feedback on:
          - Code quality and best practices
          - Potential bugs or issues
          - Performance considerations
          - Security concerns
          - Test coverage

          Be constructive and helpful in your feedback.

        # Optional: Customize review based on file types
        # prompt: |
        #   Review this PR focusing on:
        #   - For TypeScript files: Type safety and proper interface usage
        #   - For API endpoints: Security, input validation, and error handling
        #   - For React components: Performance, accessibility, and reusability
        #   - For test files: Coverage, edge cases, and test quality